services:
  - type: web
    name: klear-decision-room
    runtime: node
    plan: free
    buildCommand: npm ci
    startCommand: npm run start:hosted
    healthCheckPath: /health
    envVars:
      - key: HOST
        value: 0.0.0.0
      - key: KLEAR_MODEL_ID
        sync: false
