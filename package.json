{
  "name": "klear-decision-room",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "KLEAR Decision Room Phase 1: persistent decision cases, neutral demo data, and minimal case API.",
  "scripts": {
    "start": "node apps/api/src/server.js",
    "dev": "node apps/api/src/server.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
